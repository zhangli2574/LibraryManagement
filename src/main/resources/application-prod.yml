# 生产环境配置

# 数据源配置（示例：MySQL）
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/librarydb?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: your_secure_password
  
  # JPA 配置
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate  # 生产环境：只验证表结构，不自动创建
    show-sql: false       # 不显示 SQL 语句
    properties:
      hibernate:
        format_sql: false
  
  # H2 Console（生产环境禁用）
  h2:
    console:
      enabled: false

# 日志配置
logging:
  level:
    root: WARN
    com.kyk.librarymanagement: INFO
    org.springframework.web: WARN
  file:
    name: logs/application.log  # 日志文件路径
    max-size: 10MB              # 单个日志文件最大大小
    max-history: 30             # 保留天数
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# JWT 配置（生产环境）
jwt:
  secret: ${JWT_SECRET:prod-secure-secret-key-for-library-management-system-2026-change-me}  # 从环境变量读取
  expiration: 3600000  # 1小时（毫秒）

# 服务器配置
server:
  port: 8080
  # SSL 配置（如果需要 HTTPS）
  # ssl:
  #   enabled: true
  #   key-store: classpath:keystore.p12
  #   key-store-password: changeit
  #   key-store-type: PKCS12

# SpringDoc 配置（生产环境可选择性禁用）
springdoc:
  api-docs:
    enabled: false  # 生产环境通常禁用 API 文档
  swagger-ui:
    enabled: false  # 生产环境通常禁用 Swagger UI
