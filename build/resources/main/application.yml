# 应用配置
spring:
  application:
    name: LibraryManagement
  
  # 激活的环境配置（dev/prod）
  profiles:
    active: dev

# 服务器配置
server:
  port: 8080

# SpringDoc 配置
springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
